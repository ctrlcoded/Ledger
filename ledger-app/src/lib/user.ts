// Single source of truth for the (mock) signed-in user, so the navbar profile
// menu and the settings account card always stay in sync.
export const currentUser = {
  name: "Aryan Raghav",
  email: "aryan@ledger.app",
  initials: "AR",
};
